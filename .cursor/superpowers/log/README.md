# AI session log

Append-only журнал работы агента в репозитории. Не замена git и не дамп чата Cursor.

Файл дня: `YYYY-MM-DD.md`. Пишет **родительский** агент в конце единицы работы.

Формат блока — в `.cursor/rules/superpowers.mdc` (секция AI session log). Не логировать промпты, секреты, сырой вывод тестов.
