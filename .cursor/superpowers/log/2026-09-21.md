# 2026-09-21

## Mobile UI chrome brainstorm (parked)

- **Цель:** улучшить UI/UX Sight на телефоне без нового shell: те же виджеты, размеры и позиции.
- **Спека / план:** черновик `.cursor/superpowers/specs/2026-09-21-mobile-ui-chrome-design.md` (`status: brainstorming-parked`). As-is: `2026-09-17-sight-ui-theme-design.md` (+ viewer-crs fullscreen, map-tools tools-panel). Плана реализации нет.
- **Скилы:** `.cursor/skills/sight-a11y-touch/SKILL.md`, `.cursor/skills/sight-angular-ui/SKILL.md`; для fullscreen-инструмента ещё `.cursor/skills/sight-map-tools/SKILL.md`.
- **По существу:** два яруса (≤520 телефон, 521–767 compact A); 44px на ≤767; столбик высота+шеврон coords по центру сверху без wrapper; coords свёрнут целиком, только клик по шеврону; tools по центру снизу на телефоне; UX групп tools не менять; fullscreen → CameraViewTools как take-screenshot; Cesium-кнопку выкл.; mixin на десктопе опустить целиком.
- **Standing decisions:** нет (ещё не в as-is спеках). Макеты companion в `.superpowers/brainstorm/` (не коммитить).
- **Парковки:** плавающие окна, sidenav, landscape/safe-area, чекбокс «у курсора» на таче, разделение CSS vs touch-детектора; финальная галочка на сводку ≤520.
- **Статус:** parked / needs spec. Код не писали. Resume — brainstorming с черновика, не implementation.

## Mobile UI chrome spec (approved, awaiting user file review)

- **Цель:** нормативная спека адаптивного хрома ≤767 после закрытия brainstorm.
- **Спека / план:** `.cursor/superpowers/specs/2026-09-21-mobile-ui-chrome-design.md` (`status: approved`). Плана ещё нет — после ревью файла Evgeniy → writing-plans.
- **Скилы:** `.cursor/skills/sight-a11y-touch/SKILL.md`, `.cursor/skills/sight-angular-ui/SKILL.md`, `.cursor/skills/sight-map-tools/SKILL.md`.
- **По существу:** два детектора в одном сервисе (CSS vs UA); 44px; phone/compact chrome; окна слева при табах top-right; coords шеврон; fullscreen как screenshot; mixin целиком вниз.
- **Standing decisions:** to-be спека + ссылки в `.cursor/superpowers/README.md` и таблице фич product design. As-is ui-theme/viewer-crs/map-tools не переписывать до реализации.
- **Статус:** done (spec written). Ждём ревью файла человеком. Коммита нет.

## Mobile UI chrome plan

- **Цель:** план реализации approved спеки хрома ≤767.
- **Спека / план:** `.cursor/superpowers/specs/2026-09-21-mobile-ui-chrome-design.md` → `.cursor/superpowers/plans/2026-09-21-mobile-ui-chrome.md`.
- **Скилы:** `.cursor/skills/sight-a11y-touch/SKILL.md`, `.cursor/skills/sight-angular-ui/SKILL.md`, `.cursor/skills/sight-map-tools/SKILL.md`.
- **По существу:** 8 задач — детекторы, 44px, позиции, шеврон coords, sidenav overlay, окна слева, fullscreen-инструмент+mixin, as-is спеки.
- **Standing decisions:** коммит только по просьбе Evgeniy (шаги git в плане skip). `DeviceService` вне скоупа.
- **Статус:** done (plan written). Код не писали.

## Auto-review: subagents must not prompt for allowlisted commands

- **Цель:** субагенты не показывают карточку одобрения на уже разрешённых консольных командах, в том числе в следующих сессиях.
- **Спека / план:** не product; owner — `AGENTS.md` + `.cursor/permissions.json` + `.cursor/rules/auto-review-allowlist.mdc`; overlay `~/.cursor/permissions.json` и CLI `~/.cursor/cli-config.json`.
- **Скилы:** нет (соглашение агента, не фича карты).
- **По существу:** карточка = `request_smart_mode_approval` после блока классификатора. Правило alwaysApply запрещает флаг на allowlist. Расширены Windows-префиксы `npx.cmd` / `npm.cmd`. Если Auto-review всё же блокирует — BLOCKED родителю, не человеку.
- **Standing decisions:** да — owner files выше.
- **Статус:** done.

## Auto-review: no card for dir listing / no --no-index

- **Цель:** убрать карточку Auto-review на листинге каталогов и на `git diff --no-index`.
- **Спека / план:** не product; owner — `AGENTS.md` + `.cursor/permissions.json` + `.cursor/rules/auto-review-allowlist.mdc`; overlay `~/.cursor/permissions.json` и CLI `~/.cursor/cli-config.json`.
- **Скилы:** нет.
- **По существу:** карточка на `; Get-ChildItem` (префикса не было) и на `git diff --no-index -- /dev/null` (классификатор считает произвольным чтением файла). В allowlist: `Get-ChildItem`/`gci`. Правило: не `--no-index`, не `/dev/null`, одна allowlisted-команда на Shell.
- **Standing decisions:** да — те же owner files.
- **Статус:** done.

## Mobile UI chrome SDD (Tasks 1–8)

- **Цель:** тот же набор виджетов на ≤767: 44px, два детектора, phone overlay sidenav, chevron coords, окна слева, ToggleFullscreen, mixin вниз.
- **Спека / план:** `.cursor/superpowers/specs/2026-09-21-mobile-ui-chrome-design.md` (`status: implemented`) → `.cursor/superpowers/plans/2026-09-21-mobile-ui-chrome.md`.
- **Скилы:** `.cursor/skills/sight-a11y-touch/SKILL.md`, `.cursor/skills/sight-angular-ui/SKILL.md`, `.cursor/skills/sight-map-tools/SKILL.md`.
- **По существу:** `CheckMobileDeviceService` UA vs matchMedia; phone 46% stack; Cesium fullscreen off; as-is ui-theme/viewer-crs/map-tools + product table + a11y trap. Fix: tab `top: 111px` на ≤520.
- **Standing decisions:** `requestFullscreen`/`exitFullscreen`; Auto-review Get-ChildItem / no `--no-index`; embedded fullscreen = env. Коммит только по просьбе.
- **Статус:** done (working tree, no commit). `npx ng test --no-watch` 96 files / 244 tests.

## Mobile UI chrome visual pass (582 + icons)

- **Цель:** правки вёрстки после резолюции Evgeniy: компас, иконки/шевроны tools, sidenav toggle, phone 582, высота в одну строку, тонкий coords-шеврон.
- **Спека / план:** `.cursor/superpowers/specs/2026-09-21-mobile-ui-chrome-design.md` (phone ≤582, compact 583–767). As-is ui-theme + skill a11y.
- **Скилы:** `.cursor/skills/sight-a11y-touch/SKILL.md`, `.cursor/skills/sight-angular-ui/SKILL.md`.
- **По существу:** `mobile()`/`phoneLayout` 582; компас tablet scale 1.1; tools chevron снова ⅓ ширины; иконки `--regular-btn-svg-size`; sidenav toggle ширина `/1.5`; coords chevron высота `btn/3`; окна top 86.
- **Standing decisions:** порог phone 582 (весь хром, не только sidenav). Коммит только по просьбе.
- **Статус:** done, ждём резолюцию. Тесты 96/244.

## Mobile UI chrome visual pass 2 (tools UX + overlay 767)

- **Цель:** вернуть свёртку групп tools; центрировать иконки; sidenav overlay с tablet ≤767; подпись высоты крупнее и слева.
- **Спека:** chrome + ui-theme: `tabletLayout` ≤767 для overlay; `phoneLayout` ≤582 для столбика/coords/окон.
- **Скилы:** `.cursor/skills/sight-a11y-touch/SKILL.md`, `.cursor/skills/sight-angular-ui/SKILL.md`.
- **По существу:** снял `display: inline-flex !important` (ломал `display: none` кнопок). `npx ng test --no-watch` 96/245.
- **Статус:** done, ждём резолюцию.

## Mobile UI chrome visual pass 3 (height copy + coords overlap)

- **Цель:** телефон — подпись высоты «Высота камеры» без ужатия шрифта/инпута; шеврон coords как у tools (overlap на высоту, контент после зазора, стрелка вниз/вверх); sidenav toggle на tablet ещё `/1.5`.
- **Спека / план:** `.cursor/superpowers/specs/2026-09-21-mobile-ui-chrome-design.md`. Skill a11y: toggle `/ 2.25`.
- **Скилы:** `.cursor/skills/sight-a11y-touch/SKILL.md`, `.cursor/skills/sight-angular-ui/SKILL.md`.
- **По существу:** CSS-swap подписи; chevron `margin-top: btn*-7/96`, body `btn/3`; окна top 78; toggle `btn/2.25`.
- **Standing decisions:** нет новых (уточнение уже approved chrome). Коммит только по просьбе.
- **Статус:** done, ждём проверку Evgeniy.

## Mobile UI chrome visual pass 4 (stack width + fullscreen body)

- **Цель:** телефон — высота/шеврон `max-content`, не 46%; opacity шеврона coords 0.8; зазор `btn/9`; fullscreen не прячет chrome.
- **Спека / план:** chrome + as-is viewer-crs (`requestFullscreen(document.body)`).
- **Скилы:** `.cursor/skills/sight-a11y-touch/SKILL.md`, `.cursor/skills/sight-angular-ui/SKILL.md`, `.cursor/skills/sight-cesium-map/SKILL.md`.
- **По существу:** корень бага — `#cesiumContainer` sibling к `#sightUiContainer`; Cesium-кнопка фулскринила `document.body`.
- **Standing decisions:** нет (уточнение уже approved chrome/viewer-crs). Коммит только по просьбе.
- **Статус:** done, ждём проверку Evgeniy.

## Mobile UI chrome visual pass 5 (compass axis + fullscreen icons)

- **Цель:** на tablet выровнять вертикальные оси компаса и `+/⌂/−`; иконка/тултип fullscreen enter↔exit.
- **Спека:** chrome + viewer-crs (тултип «На весь экран» / «Выйти из полноэкранного режима»).
- **Скилы:** `.cursor/skills/sight-a11y-touch/SKILL.md`, `.cursor/skills/sight-angular-ui/SKILL.md`.
- **По существу:** `.navigation-controls { right: 45px }` после базового правила; `fullscreenchange` → signal.
- **Статус:** done. Коммит только по просьбе.


