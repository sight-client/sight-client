# 2026-09-23

## Docs: next mobile UI improvement

- **Цель:** подтянуть контракт под коммит `04192d8` («next mobile UI improvement»).
- **Спека:** `.cursor/superpowers/specs/2026-09-21-mobile-ui-chrome-design.md`; as-is ui-theme, map-tools, product index.
- **Скилы:** `.cursor/skills/sight-a11y-touch/SKILL.md`.
- **По существу:** `mobile` / `phoneLayout` ≤460; кнопки 44px и шеврон ½ с `laptop` ≤1080; `tools-panel` по центру с `tablet` ≤767; overlay sidenav только UA mobile при ширине ≤767 (`wideMobile`); свайп вправо и влево. План `2026-09-21-mobile-ui-chrome` не переписывал — он executed.
- **Статус:** done.

## Breakpoints out of common-core

- **Цель:** media-правила из `common-core.scss` рядом с селектором. Продолжение parked-блока 2026-09-22.
- **Скилы:** `.cursor/skills/sight-angular-ui/SKILL.md`, `.cursor/skills/sight-a11y-touch/SKILL.md`.
- **По существу:** sidenav и шапка → `planet.scss`; высота и подписи → `camera-height-tool`; tools (`laptop` 194px, центр с `tablet`) → `tools-panel.scss`; вкладки и `1660px` → `floating-windows-tabs-panel.scss`; ширина/top/visibility coords → `cursor-coords-info.scss`. В `common-core` остался якорь стека телефона (`left: 50%` и `phone-stack-ready`). Пустые desktop/tiny убраны. Поведение то же.
- **Standing decisions:** нет.
- **Статус:** done.

## Close: tablet overlay sidenav + doc check

- **Цель:** закрыть задачу. Плана нет; `2026-09-21-mobile-ui-chrome` уже executed, не перештамповывал.
- **Спека:** `.cursor/superpowers/specs/2026-09-17-sight-ui-theme-design.md`, `.cursor/superpowers/specs/2026-09-21-mobile-ui-chrome-design.md`, индекс в product spec.
- **Скилы:** `.cursor/skills/sight-a11y-touch/SKILL.md`.
- **По существу:** sidenav `over` и старт закрыт на ≤767, включая десктопный UA. Полоска свайпа при `isMobile` или `tabletLayout`.
- **Standing decisions:** да — закрытие сверяет as-is спеки и `sight-*` скилы, не только планы. Owner: `AGENTS.md`, `.cursor/rules/superpowers.mdc`.
- **Статус:** done.

## TS quality refactor, first slices

- **Цель:** планы на пять согласованных рефакторингов и первые безопасные шаги.
- **Планы:** `.cursor/superpowers/plans/2026-09-23-tool-panel-shared-button.md`, `2026-09-23-error-reporting.md`, `2026-09-23-user-data-auth-flow.md`, `2026-09-23-type-holes.md`, `2026-09-23-cursor-canvas-resize.md`.
- **Скилы:** `.cursor/skills/sight-angular-ui/SKILL.md`, `.cursor/skills/sight-map-tools/SKILL.md`, `.cursor/skills/sight-testing/SKILL.md`, `.cursor/skills/sight-cesium-map/SKILL.md`.
- **По существу:** `ToolButtonVisibility` на кнопках панели; `reportError` и `GlobalErrorHandlerService` без `chalk`; ресайз канваса через `fromEvent`, не `@HostListener` на сервисе. Авторизация, замена `any` и одна модель групп панели ещё в планах.
- **Standing decisions:** нет.
- **Статус:** done для этих трёх шагов. Планы не закрыты.

## Pause until tomorrow

- **Цель:** Евгений остановил сессию. Продолжить оставшиеся задачи тех же пяти планов, не переспрашивая согласие на темы.
- **Планы:** те же `2026-09-23-*`. Не ставить `executed`.
- **По существу:** дальше — одна модель групп в `tools-panel`, затем вычищение `chalk`, затем `UserDataService`, затем дыры типов. Коммита нет.
- **Статус:** parked.
