# 2026-09-24

## toolName stays a literal

- **Цель:** не давать агенту расширять `toolName` до `string`.
- **Спека:** `.cursor/superpowers/specs/2026-09-17-sight-map-tools-design.md`.
- **Скилы:** `.cursor/skills/sight-map-tools/SKILL.md`.
- **По существу:** `toolName` — ключ стора и `windowName`. Тип — union литералов группы: `DrawingToolName`, `MeasuringToolName`, `CameraToolName`. Новый инструмент добавляет литерал в массив имён, тип не ослаблять.
- **Standing decisions:** да. Owner: `sight-map-tools` и as-is map-tools spec.
- **Статус:** done.

## Classes stay classes

- **Цель:** при рефакторинге не превращать классы Евгения в интерфейсы и прочие стёртые типы.
- **Скилы:** нет нового. Owner: `AGENTS.md`, `.cursor/rules/angular-conventions.mdc`.
- **По существу:** класс не превращать в интерфейс даже без `instanceof` в файле. Проверку Евгений может добавить позже. Пример: `UserRegistrationData`.
- **Standing decisions:** да.
- **Статус:** done.

## Type-quality pass, paused

- **Цель:** продолжить проход по дырам типов в `src/**/*.ts` без `strictPropertyInitialization`, без расширения `toolName` и без превращения классов Евгения в стёртые типы.
- **Планы:** `.cursor/superpowers/plans/2026-09-23-type-holes.md` и соседние планы того же прохода. Не штамповать `executed`.
- **Скилы:** `.cursor/skills/sight-map-tools/SKILL.md`, `.cursor/skills/sight-geodesy/SKILL.md`, `.cursor/skills/sight-testing/SKILL.md`.
- **По существу:** `descriptionsCoords` — `PositionCoordsDescription`. `getPositionCoordsNumbers` возвращает `PositionCoordsNumbers` (строки). KML-свойства читаются через `in`, не `as Record`. Зона СК-42 м после автозоны — конечное число, иначе пустая SRS. Кэш GET без `as HttpEvent`. `isCRS` / `isDrawingToolName` / `isMeasuringToolName` / `isCameraToolName` через `some`, не `as readonly string[]`. Ластик в запасном пути ищет одиночную сущность по `entity.id`. ODS берёт позиции и радиус только после проверки типа; мёртвая ветка `addDome` внутри круга убрана. `getCircle` требует `Cartesian3` и числовые полуоси. Курсор инструментов идёт через `cartesianFromProperty`.
- **Дальше:** живые `getValue()` ещё остаются в KML, подписях и материалах. `WindowName` остаётся `string`. `{} as CustomViewer`, касты `prototype.update` и `null as T` / `undefined as T` не трогать. `chalk` ещё в lockfile, пока Евгений не запустит `npm install`.
- **Статус:** done (пауза вечером 24-го; закрыто 2026-09-25, см. `2026-09-25.md`).
