# 2026-09-25

## Close: TypeScript quality plans

- **Цель:** закрыть пять планов качества TypeScript после полной сюиты и дымовой проверки карты. Снимает parked-блок «Type-quality pass, paused» в `2026-09-24.md`.
- **Планы:** `.cursor/superpowers/plans/2026-09-23-tool-panel-shared-button.md`, `2026-09-23-error-reporting.md`, `2026-09-23-user-data-auth-flow.md`, `2026-09-23-type-holes.md`, `2026-09-23-cursor-canvas-resize.md` — **executed** 2026-09-25, не гонять заново.
- **Спека:** `.cursor/superpowers/specs/2026-09-21-mobile-ui-chrome-design.md` (убран `MutationObserver` у кнопки полноэкрана); `.cursor/superpowers/specs/2026-09-17-sight-map-tools-design.md` (видимость хостов, `getRus` / `getOrigin`).
- **Скилы:** `.cursor/skills/sight-map-tools/SKILL.md`, `.cursor/skills/sight-cesium-map/SKILL.md`, `.cursor/skills/sight-angular-ui/SKILL.md`, `.cursor/skills/sight-testing/SKILL.md`.
- **По существу:** `npx ng test --no-watch` — 97 файлов, 275 тестов. Карта на `http://localhost:9002/sight-client/` открылась: холст, список чертежей, 14 кнопок панели, СК WGS-84, меню пользователя. `strictPropertyInitialization` не включали. `chalk` ещё может быть в lockfile до `npm install`. `WindowName` остаётся `string`. `{} as CustomViewer` и касты `prototype.update` не трогали.
- **Standing decisions:** без новых. Owner-файлы уже держат `toolName`-литералы и «классы Евгения остаются классами».
- **Статус:** done.

## Desktop height bar between coords and tools

- **Цель:** на ширинах >1080 панель высоты не наезжает на tools; стоит по центру зазора coords↔tools, tools сдвинуты на 50px вправо.
- **Спека:** `.cursor/superpowers/specs/2026-09-17-sight-ui-theme-design.md` (строка >1080). Плана нет.
- **Скилы:** `.cursor/skills/sight-angular-ui/SKILL.md`, `.cursor/skills/sight-a11y-touch/SKILL.md`.
- **По существу:** токены `--coords-panel-*` / `--tools-panel-left` в `common-core`; высота `calc` + `translateX(-50%)`; laptop `transform: none`. На 1100px зазоры 57px с обеих сторон; при font-size 18px — 26px, без наезда. 768–1080 без изменений (`left: 86px` сверху, tools `194px`).
- **Standing decisions:** позиции нижней полосы desktop — CSS-переменные в `common-core`, не магические `200`/`460`. Owner: ui-theme spec + `sight-angular-ui`.
- **Статус:** done.

## Close: desktop height bar

- **Цель:** закрыть правку нижней полосы >1080 без штампа плана.
- **Планы:** нет. Не ставить `executed` ни на один файл в `plans/`.
- **Спека / скил:** `.cursor/superpowers/specs/2026-09-17-sight-ui-theme-design.md` и `.cursor/skills/sight-angular-ui/SKILL.md` уже совпадают с кодом; повторно не правились. `sight-a11y-touch` не меняли (`460px` там — брейкпоинт телефона).
- **Статус:** done.
