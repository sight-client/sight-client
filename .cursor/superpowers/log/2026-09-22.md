# 2026-09-22

## Phone coords/tools chevron thickness

- **Цель:** на ≤582 увеличить толщину шеврона coords и шевронов групп tools в 1.5 раза (удобнее тап).
- **Спека:** `.cursor/superpowers/specs/2026-09-21-mobile-ui-chrome-design.md` (`btn/2` на телефоне).
- **Скилы:** `.cursor/skills/sight-a11y-touch/SKILL.md`, `.cursor/skills/sight-angular-ui/SKILL.md`.
- **По существу:** coords `height: btn/2`; tools на `mobile()` `width: btn/2` (после rotate это визуальная толщина); окна top 86.
- **Статус:** done. Коммит только по просьбе.

## Tools chevron: keep axis when thickening

- **Цель:** толщина `btn/2` без сдвига вправо.
- **По существу:** `rotate(90deg)` + больший `width` двигает центр; компенсировал `margin-left: 25/96`.
- **Статус:** done.

## Tool chevron thickness token

- **Цель:** отступы шеврона tools считаются из `--tool-chevron-thickness`, без ручной подгонки `margin-left`.
- **По существу:** `margin-left = 49/96×btn − T/2`, `margin-bottom = T/2 − 55/96×btn`; на mobile T=`btn/2`. Coords height и tabs top берут тот же токен.
- **Статус:** done.

## Coords chevron glued like tools

- **Цель:** шеврон coords визуально приклеен к панели высоты, как шевроны групп к кнопкам tools.
- **Спека:** `.cursor/superpowers/specs/2026-09-21-mobile-ui-chrome-design.md`.
- **Скилы:** `.cursor/skills/sight-a11y-touch/SKILL.md`, `.cursor/skills/sight-angular-ui/SKILL.md`.
- **По существу:** верх квадратный + `border-top` (`--theme-border-color`); низ `6px`; overlap `-7/96` без изменений. Панель высоты не сплющивал.
- **Статус:** done. Коммит только по просьбе.

## CRS select vertical center at 44px

- **Цель:** имя выбранной СК снова по центру высоты селекта, когда поле растёт до 44px (≤767).
- **Скилы:** `.cursor/skills/sight-a11y-touch/SKILL.md`, `.cursor/skills/sight-angular-ui/SKILL.md`.
- **По существу:** убрал `margin-top: 4px` (подгонка под 32px); infix `min-height`/`height` = `--regular-btn-size`, flex `align-items: center`.
- **Статус:** done. Коммит только по просьбе.

## Phone coords expanded two-column

- **Цель:** на ≤582 раскрытая панель coords ниже по высоте: две колонки 1fr/1fr (шир/долг слева, чекбокс справа).
- **Спека:** `.cursor/superpowers/specs/2026-09-21-mobile-ui-chrome-design.md`.
- **Скилы:** `.cursor/skills/sight-a11y-touch/SKILL.md`, `.cursor/skills/sight-angular-ui/SKILL.md`.
- **По существу:** обёртка `.coords-latlon`; grid только в `mobile()`. Ширину стека не увеличивал — WGS-84 7 знаков влезает. Select на всю ширину. ≤767 без колонок.
- **Статус:** done. Коммит только по просьбе.

## Phone height label h: and stack width

- **Цель:** на ≤582 подпись высоты «h:», ширина стека по контенту без скейла шрифта/инпута; coords/шеврон той же ширины, текст шир/долг влезает.
- **Спека:** `.cursor/superpowers/specs/2026-09-21-mobile-ui-chrome-design.md`.
- **Скилы:** `.cursor/skills/sight-a11y-touch/SKILL.md`, `.cursor/skills/sight-angular-ui/SKILL.md`.
- **По существу:** short label `h:`; coords grid `1fr auto`; `min-width` высоты из `--phone-coords-content-width` (scrollWidth строк + чекбокс). Свёрнут — снова max-content «h:».
- **Статус:** done. Коммит только по просьбе.

## Phone coords cursor icon vs checkbox

- **Цель:** иконка курсора не наезжает на чекбокс, как до ужатия; стек шире на этот зазор.
- **По существу:** убрал compact 18px у mat-checkbox; иконка 30px, `margin-left: -8px` как раньше. Ширина стека ~170px (высота/шеврон/select).
- **Статус:** done. Коммит только по просьбе.

## Phone stack: height, chevron, coords same width

- **Цель:** шеврон coords и панель высоты той же ширины, что раскрытые coords.
- **По существу:** `--phone-height-stack-width` сразу на высоту и `.coords-container-main`; observer `max(height, coordsMin)`. Карточка coords `width: 100%`.
- **Статус:** done. Коммит только по просьбе.

## Phone stack width stable when collapsed

- **Цель:** шеврон и панель высоты уже в ширине раскрытых coords, без скачка при клике на шеврон.
- **По существу:** тело coords всегда в DOM, свёртка через `visibility: hidden` + `aria-hidden`/`inert`; ширину меряем сразу и не сбрасываем.
- **Статус:** done. Коммит только по просьбе.

## Phone stack: min coords card width, no center gap

- **Цель:** высота/шеврон/coords по минимальной ширине карточки шир/долг+чекбокс, без пустоты в центре и без скачка при раскрытии.
- **Спека:** `.cursor/superpowers/specs/2026-09-21-mobile-ui-chrome-design.md`.
- **Скилы:** `.cursor/skills/sight-a11y-touch/SKILL.md`, `.cursor/skills/sight-angular-ui/SKILL.md`.
- **По существу:** мера `max-content` карточки, не `p.scrollWidth` растянутой `1fr`; стек = max(натуральная высота, карточка), без ratchet; infix селекта без 180px; grid `max-content auto`.
- **Статус:** done. Коммит только по просьбе.

## Phone coords: restore cursor icon in stack width

- **Цель:** иконка курсора снова видна у чекбокса; ширина стека по этой карточке, без пустоты в центре.
- **Спека:** `.cursor/superpowers/specs/2026-09-21-mobile-ui-chrome-design.md`.
- **Скилы:** `.cursor/skills/sight-a11y-touch/SKILL.md`, `.cursor/skills/sight-angular-ui/SKILL.md`.
- **По существу:** замер не режется `max-width: 100%` родителя 138px; в ширину входит SVG; на телефоне `overflow: visible`. Стек ~172px.
- **Статус:** done. Коммит только по просьбе.

## Phone stack: keep width when CRS is СК-42 м

- **Цель:** при выборе «СК-42 м» стек не сужается; не раздувать ширину десктопной подписью «Высота наблюдения».
- **Спека:** `.cursor/superpowers/specs/2026-09-21-mobile-ui-chrome-design.md`.
- **Скилы:** `.cursor/skills/sight-a11y-touch/SKILL.md`, `.cursor/skills/sight-angular-ui/SKILL.md`.
- **По существу:** `--phone-coords-content-width` только растёт; стек = max(h: натуральная, coordsMin), только при `phoneLayout`. На 390px WGS-84 и СК-42 м обе 172px.
- **Статус:** done. Коммит только по просьбе.

## Phone height: no grow-on-load

- **Цель:** панель высоты при загрузке уже в ширине стека, без скачка с «h:» на карточку coords.
- **Спека:** `.cursor/superpowers/specs/2026-09-21-mobile-ui-chrome-design.md`.
- **Скилы:** `.cursor/skills/sight-a11y-touch/SKILL.md`, `.cursor/skills/sight-angular-ui/SKILL.md`.
- **По существу:** карточка coords в DOM на телефоне и без живых координат (пробы WGS-84); observer не пишет стек из одной высоты; показ после `phone-stack-ready`.
- **Статус:** done. Коммит только по просьбе.

## Phone chevron waits for height panel (CSS)

- **Цель:** шеврон coords не висит в воздухе до панели высоты.
- **Спека:** `.cursor/superpowers/specs/2026-09-21-mobile-ui-chrome-design.md`.
- **Скилы:** `.cursor/skills/sight-a11y-touch/SKILL.md`, `.cursor/skills/sight-angular-ui/SKILL.md`.
- **По существу:** только CSS — `.coords-container-main` `visibility: visible` при `:has(.camera-height-container)` (высота `@defer (on idle)`).
- **Статус:** done. Коммит только по просьбе.

## Standing: gitignore and delete `.superpowers/` brainstorm companion

- **Цель:** макеты visual companion не в репо; после brainstorm удалять.
- **Скилы:** нет (процесс Superpowers).
- **По существу:** `.gitignore` `.superpowers/`; правило в `AGENTS.md` и `.cursor/rules/superpowers.mdc`; каталог удалён. Не путать с `.cursor/superpowers/`.
- **Статус:** done. Коммит только по просьбе.

## Session wrap: mobile UI chrome + companion cleanup

- **Цель:** зафиксировать финальный хром ≤582 и процесс `.superpowers/`.
- **Спека:** `.cursor/superpowers/specs/2026-09-21-mobile-ui-chrome-design.md`; as-is ui-theme / viewer-crs / map-tools.
- **Скилы:** `.cursor/skills/sight-a11y-touch/SKILL.md`, `.cursor/skills/sight-angular-ui/SKILL.md`.
- **По существу:** подпись высоты «Обзор с:»; полы `body` 350×440 оставить; ловушки стека в скиле a11y-touch. Коммит только по просьбе.
- **Статус:** done.

## Mobile UI chrome plan marked executed

- **Цель:** план хрома не выглядел незакрытым после Tasks 1–8 и визуальных правок 21–22 сентября.
- **Спека / план:** `.cursor/superpowers/specs/2026-09-21-mobile-ui-chrome-design.md` (`status: implemented`); `.cursor/superpowers/plans/2026-09-21-mobile-ui-chrome.md`.
- **Скилы:** `.cursor/skills/sight-a11y-touch/SKILL.md`.
- **По существу:** баннер `executed 2026-09-22` на плане; строка в README; скил a11y-touch. Чекбоксы плана не трогал — как у executed-планов тестов.
- **Standing decisions:** executed-план хрома не гонять заново — owner: план, README, спека chrome, `sight-a11y-touch`.
- **Статус:** done.

## Standing: close a plan only when Evgeniy says so

- **Цель:** агент ставит `executed` по смыслу реплики в чате, не по зелёным тестам и не по своему `done` в журнале.
- **Спека / план:** нет. Owner: `.cursor/rules/superpowers.mdc`, короткая отсылка в `AGENTS.md`.
- **Скилы:** нет (процесс Superpowers).
- **По существу:** чеклист баннера, README, to-be спеки, скила и журнала. «Ок» на один дифф план не закрывает. «Все планы» — только планы этой единицы работы.
- **Standing decisions:** да — те же owner files.
- **Статус:** done.

## Standing: ask before stamping a plan executed

- **Цель:** перед правкой документации агент называет планы и ждёт подтверждения.
- **Спека / план:** нет. Owner: `.cursor/rules/superpowers.mdc`, отсылка в `AGENTS.md`.
- **Скилы:** нет.
- **По существу:** фраза «закрывай план» — повод спросить, не писать файлы. «Да» на этот вопрос — разрешение. Повторно не спрашивать.
- **Standing decisions:** да — те же owner files.
- **Статус:** done.

## Sidenav edge swipe on overlay

- **Цель:** на ≤767 открывать свёрнутую левую панель свайпом одним пальцем слева направо от левого края.
- **Спека / план:** as-is `.cursor/superpowers/specs/2026-09-17-sight-ui-theme-design.md`; хром `.cursor/superpowers/specs/2026-09-21-mobile-ui-chrome-design.md`. Отдельного плана нет.
- **Скилы:** `.cursor/skills/sight-a11y-touch/SKILL.md`, `.cursor/skills/sight-angular-ui/SKILL.md`.
- **По существу:** полоска `.sight-main-sidenav-edge-swipe` в `planet` (24px + safe-area), порог 48px и сильнее по горизонтали; шеврон и шапка поверх. Не `touchmove` на `document`.
- **Standing decisions:** жест в as-is ui-theme и в спеке хрома; ловушка в `sight-a11y-touch`.
- **Статус:** done.
