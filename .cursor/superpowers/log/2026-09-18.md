# 2026-09-18

## SK-42 m reverse autozone (ODS import)

- **Цель:** `getPseudoEntitiesFromReport` → `CoordSystems.toWGS84Cartographic('СК-42 м', …, '')` не должен давать NaN / «normalized result is not a number».
- **Спека / план:** viewer-crs as-is. Плана нет.
- **Скилы:** `.cursor/skills/sight-geodesy/SKILL.md`, `.cursor/skills/sight-testing/SKILL.md`.
- **По существу:** автозона СК-42 м кормила GK-метры (поля lon/lat отчёта) в krass longlat. Зону для метров берём из easting `floor(lon/1e6)`. `coord-sistems.lib.ts`, spec, ловушка в `sight-geodesy`.
- **Standing decisions:** обратный ход СК-42 м с метрами в CartographicLike — зона из easting, не из longlat. Скил геодезии.
- **Статус:** done. Karma: 4/4 SUCCESS на `coord-sistems.lib.spec.ts`. Полный `ng test` по-прежнему не собирается из-за старых ошибок в других spec.

## Vitest 4.1 unit-test runner

- **Цель:** сменить unit-test раннер с Karma+Jasmine на Vitest 4.1; spec-сюиту не переписывать в этой сессии.
- **Спека / план:** as-is product + GitHub Pages + UI theme. Плана нет (bounded).
- **Скилы:** `.cursor/skills/sight-change-control/SKILL.md`, `.cursor/skills/sight-testing/SKILL.md`.
- **По существу:** `@angular/build:unit-test` + jsdom; build-конфиг `testing` → `environment.test.ts`. Сняты karma/jasmine. Обвязка: `AGENTS.md`, `angular-conventions.mdc`, скилы, product/GitHub Pages/UI specs.
- **Standing decisions:** раннер = Vitest 4.1 через `ng test`. Не Karma. Vitest 5+ и Playwright — со спросом. `toBe(true)`, не `toBeTrue()`.
- **Статус:** done. Smoke: `coord-sistems.lib.spec.ts` 4/4. Полный `ng test --no-watch`: 26 passed / 67 failed / 3 unhandled (старые stub-spec).

## Unit-test suite strategy (plans only)

- **Цель:** контракт покрытия Vitest-сюиты и шесть планов; spec-файлы тестов в этой сессии не писать.
- **Спека / план:** `.cursor/superpowers/specs/2026-09-18-sight-unit-test-suite-design.md` (process, не product-child). Планы `2026-09-18-unit-test-*.md` (hygiene → ui-theme → viewer-crs → drawing → measuring → list/export/windows).
- **Скилы:** `.cursor/skills/sight-testing/SKILL.md`, `.cursor/skills/sight-change-control/SKILL.md`.
- **По существу:** поведение, не `%`; характеристика as-is; без `Cesium.Viewer`; autofocus в сюите; blank/proxy exclude; баги парковать.
- **Standing decisions:** спека тестов не в таблице фич продукта; обновлены README process-блок и `sight-testing`.
- **Статус:** needs spec — пользователь ревьюит draft-спеку и планы до исполнительной сессии.

## Unit-test hygiene (plan 0 executed)

- **Цель:** зелёный `npx ng test --no-watch` на stub-spec без Cesium.Viewer.
- **Спека / план:** `.cursor/superpowers/specs/2026-09-18-sight-unit-test-suite-design.md`, `.cursor/superpowers/plans/2026-09-18-unit-test-hygiene.md`.
- **Скилы:** `.cursor/skills/sight-testing/SKILL.md`, `.cursor/skills/sight-change-control/SKILL.md`.
- **По существу:** rename caching spec; Autofocus в сюите; zoneless TestBeds; фейк ViewerService; ShowProgress HTTP testing; env не собирается как spec.
- **Standing decisions:** `src/environments/**` в `test.exclude`; скил `sight-testing`.
- **Статус:** done. `npx ng test --no-watch`: 91 files / 95 passed / 0 failed.

## Agent Auto-review permissions

- **Цель:** меньше подтверждений на безопасных shell-командах субагентов, без автозапуска push/deploy.
- **Спека / план:** нет (standing decision).
- **Скилы:** `.cursor/skills/sight-change-control/SKILL.md`.
- **По существу:** `~/.cursor/permissions.json` (все проекты) + `.cursor/permissions.json` (репо). CLI: `approvalMode: auto-review` в `cli-config.json`. Узкий terminalAllowlist, не префикс `git`.
- **Standing decisions:** Auto-review + allowlist в `.cursor/permissions.json`; правило в `AGENTS.md`.
- **Статус:** done. IDE Run Mode нужно один раз выбрать Auto-review в Settings → Agents → Approvals & Execution.

