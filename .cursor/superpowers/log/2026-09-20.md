# 2026-09-20

## Unit-test drawing tools (plan 1 executed)

- **Цель:** Характеризация drawing store API, русских имён инструментов, флагов ластика и активации пяти drawing tools без Cesium.Viewer.
- **Спека / план:** `.cursor/superpowers/specs/2026-09-18-sight-unit-test-suite-design.md`, `.cursor/superpowers/plans/2026-09-18-unit-test-drawing-tools.md`.
- **Скилы:** `.cursor/skills/sight-testing/SKILL.md`, `.cursor/skills/sight-map-tools/SKILL.md`.
- **По существу:** имена инструментов; `pushGroupWithoutTemporal` / cleanup сторов; `storesAreEmpty` ластика; активация drawMark–drawPolygon через fake ToolsService; `drawingsBlocker` и `buttons-subgroups-visibility`.
- **Standing decisions:** parent (не субагент) гоняет `npx ng test` по `.cursor/permissions.json`; субагент не ретраит allowlisted команды через approval card. `AGENTS.md` + `permissions.json`.
- **Парковки:** `storesAreEmpty` не смотрит MeasureService; `isHeatmap` алиасит dome; `cancelDrawingTool` Esc-path без DataSource не покрыт.
- **Статус:** done. `npx ng test --no-watch`: 92 files / 134 passed. HEAD `cc746e7`.

## Unit-test list, export, floating windows (plan 2 executed)

- **Цель:** Характеризация drawings-list, KML/ODS границ и стека плавающих окон без Viewer и без file picker UI.
- **Спека / план:** `.cursor/superpowers/specs/2026-09-18-sight-unit-test-suite-design.md`, `.cursor/superpowers/plans/2026-09-18-unit-test-list-export-windows.md`.
- **Скилы:** `.cursor/skills/sight-testing/SKILL.md`, `.cursor/skills/sight-map-tools/SKILL.md`, `.cursor/skills/sight-client-security/SKILL.md`.
- **По существу:** floatingWindowsList по `drawMark`; DrawingsListService start/setActive/changeLineCounter; KML export TestMark + parked unsanitized `javascript:` name; ODS Developer error + one-mark download + SK-42 m finite Cartesian3.
- **Парковки:** нет public rename; KML без DOMPurify (`javascript:` в name проходит `prepareKmlEntities`; `<script>` в HTML description съедается DOMParser, не санитайзером); `formReport` никогда не `return null` — ветка `'Сущшости для ods-документа отсутствуют'` недостижима; `storesAreEmpty` без MeasureService (drawing-tools).
- **Статус:** done. `npx ng test --no-watch`: 92 files / 151 passed. HEAD `419c27b`.

## Session note for next agent

Continue **one at a time** (hygiene, drawing-tools, list-export done):

1. `.cursor/superpowers/plans/2026-09-18-unit-test-measuring-tools.md` ← next
2. `.cursor/superpowers/plans/2026-09-18-unit-test-ui-theme.md`
3. `.cursor/superpowers/plans/2026-09-18-unit-test-viewer-crs.md`

SDD: implementer writes specs only. Parent runs `npx ng test` and commits. Do not `request_smart_mode_approval` on allowlisted commands. Characterization as-is; production frozen. Ledger: `.superpowers/sdd/progress.md`. Branch: `dev-with-cursor-ai`.

OnPush tests: `fixture.componentRef.setInput(...)` before `detectChanges` — not a production bug.

## Unit-test measuring tools (plan 3 executed)

- **Цель:** Характеризация geodesic/Turf helpers, русских имён измерений, сторов MeasureService и активации четырёх measuring tools без Cesium.Viewer.
- **Спека / план:** `.cursor/superpowers/specs/2026-09-18-sight-unit-test-suite-design.md`, `.cursor/superpowers/plans/2026-09-18-unit-test-measuring-tools.md`.
- **Скилы:** `.cursor/skills/sight-testing/SKILL.md`, `.cursor/skills/sight-map-tools/SKILL.md`, `.cursor/skills/sight-geodesy/SKILL.md`.
- **По существу:** `basic-measure-calculations.lib`; `measuringToolsNames` / `getRusMeasuringToolName`; `pushGroupWithoutTemporal` / `isLines` / cleanup; активация calculateLine–calculatePolygon через fake ToolsService.
- **Standing decisions:** Task-субагенты продолжали слать Auto-review карточки на allowlisted `npx ng test`. Дальше parent сам пишет spec, гоняет тесты и коммитит; `request_smart_mode_approval` не ставить.
- **Парковки:** нет публичного метода CalculateLineService «два Cartesian → строка длины» без globe (`ViewerService.calculatePosDistances` async); `removeListener` курсора по-прежнему добавляет новую функцию (ui-theme).
- **Статус:** done. HEAD `128c005`. Full suite at that point 93/181.

## Unit-test UI / theme (plan 4 executed)

- **Цель:** Характеризация theme localStorage, device/spinner/error/cursor, HTTP interceptors, Autofocus, common-global.lib, App/theme UI.
- **Спека / план:** `.cursor/superpowers/plans/2026-09-18-unit-test-ui-theme.md`.
- **Скилы:** `.cursor/skills/sight-testing/SKILL.md`, `.cursor/skills/sight-angular-ui/SKILL.md`, `.cursor/skills/sight-client-security/SKILL.md`.
- **По существу:** colorScheme light без rewrite; setUserTheme azure-blue fallback; interceptors (DOMPurify POST, double-request, cache Map-by-reference miss, spinner finalize); RoutingErrorsListener; downloadBlob click.
- **Парковки:** GET-cache ключ — object reference, hit не случается; Autofocus `[appAutoFocus]=false` всё равно фокусирует (setter без getter, ngOnInit видит undefined); `removeListener` курсора не снимает исходный handler; ProgressSpinner шаблон не биндит `isShowSpinner` (видимость в `app.html` `@if`).
- **Статус:** done. HEAD `ebd0645`. Full suite 94/210.

## Unit-test viewer / CRS (plan 5 executed)

- **Цель:** CRS round-trip, humanify, ViewerService sceneMode без Viewer, cursor desktop, morphTo2D + localStorage, run-viewer не зовёт imagery пока `viewerHasLoaded` false.
- **Спека / план:** `.cursor/superpowers/plans/2026-09-18-unit-test-viewer-crs.md`.
- **Скилы:** `.cursor/skills/sight-testing/SKILL.md`, `.cursor/skills/sight-geodesy/SKILL.md`, `.cursor/skills/sight-cesium-map/SKILL.md`.
- **По существу:** WGS-84 / СК-42 ° / ПЗ-90.11 / SK-42 m easting zone; humanify русские единицы; sceneMode 2D/3D/Columbus; `startCursorCoordsService` throws без canvas.
- **Парковки:** fly-around activation не гоняли (setFlyingAround трогает globe handler); distanceM переключает км с 2000 м, не с 1000.
- **Статус:** done. `npx ng test --no-watch`: 95 files / 229 passed. HEAD `8e2cb41`. All five coverage plans complete (hygiene skipped as already green).

## CI unit tests + KML sanitizer

- **Цель:** гонять сюиту на GitHub Actions; закрыть parked XSS `javascript:` в имени/картинке KML.
- **Спека / план:** `.cursor/superpowers/specs/2026-09-17-sight-github-pages-design.md`, `.cursor/superpowers/specs/2026-09-17-sight-map-tools-design.md`, `.cursor/superpowers/plans/2026-09-20-ci-and-kml-sanitize.md`.
- **Скилы:** `.cursor/skills/sight-testing/SKILL.md`, `.cursor/skills/sight-client-security/SKILL.md`, `.cursor/skills/sight-change-control/SKILL.md`.
- **По существу:** workflow Node 22 `npm ci` + `npx ng test --no-watch`; `prepareKmlEntities` санитайзит `name` и `billboard.image` (DOMPurify без тегов + отсев `javascript:` / `data:text/html`). `package-lock.json` уже был в git; из `.gitignore` убран.
- **Парковки:** ластик без MeasureService; Autofocus false; GET-cache по ссылке; `removeListener` курсора.
- **Статус:** done. Full suite 95 files / 231 passed.

## Coverage plans marked executed

- **Цель:** планы 0–5 и CI/KML — история, не очередь. Агенты не гоняют их заново.
- **Спека / план:** `.cursor/superpowers/specs/2026-09-18-sight-unit-test-suite-design.md` (таблица Status), `.cursor/superpowers/README.md`, `.cursor/skills/sight-testing/SKILL.md`, баннеры на `2026-09-18-unit-test-*.md` и `2026-09-20-ci-and-kml-sanitize.md`.
- **Скилы:** `.cursor/skills/sight-testing/SKILL.md`.
- **По существу:** файлы планов оставлены. Новое покрытие — отдельный план или TDD по parked-багу.
- **Standing decisions:** executed characterization plans are history; `sight-testing` SKILL.md.
- **Статус:** done.

