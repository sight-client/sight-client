# 2026-09-20

## Unit-test drawing tools (plan 1 executed)

- **Цель:** Характеризация drawing store API, русских имён инструментов, флагов ластика и активации пяти drawing tools без Cesium.Viewer.
- **Спека / план:** `.cursor/superpowers/specs/2026-09-18-sight-unit-test-suite-design.md`, `.cursor/superpowers/plans/2026-09-18-unit-test-drawing-tools.md`.
- **Скилы:** `.cursor/skills/sight-testing/SKILL.md`, `.cursor/skills/sight-map-tools/SKILL.md`.
- **По существу:** имена инструментов; `pushGroupWithoutTemporal` / cleanup сторов; `storesAreEmpty` ластика; активация drawMark–drawPolygon через fake ToolsService; `drawingsBlocker` и `buttons-subgroups-visibility`.
- **Standing decisions:** parent (не субагент) гоняет `npx ng test` по `.cursor/permissions.json`; субагент не ретраит allowlisted команды через approval card. `AGENTS.md` + `permissions.json`.
- **Парковки:** `storesAreEmpty` не смотрит MeasureService; `isHeatmap` алиасит dome; `cancelDrawingTool` Esc-path без DataSource не покрыт.
- **Статус:** done. `npx ng test --no-watch`: 92 files / 134 passed. HEAD `cc746e7`.

## Session note for next agent

Continue remaining coverage plans **one at a time** (hygiene already done; drawing-tools done):

1. `.cursor/superpowers/plans/2026-09-18-unit-test-list-export-windows.md` ← **resume at Task 3** (KML). Tasks 1–2 committed (`baf61ef`, `751640e`).
2. `.cursor/superpowers/plans/2026-09-18-unit-test-measuring-tools.md`
3. `.cursor/superpowers/plans/2026-09-18-unit-test-ui-theme.md`
4. `.cursor/superpowers/plans/2026-09-18-unit-test-viewer-crs.md`

SDD: implementer writes specs only. Parent runs `npx ng test` and commits. Do not `request_smart_mode_approval` on allowlisted commands. Characterization as-is; production frozen; park bugs. Ledger: `.superpowers/sdd/progress.md`. Branch: `dev-with-cursor-ai`.

List-export Task 3 skills: `.cursor/skills/sight-testing/SKILL.md`, `.cursor/skills/sight-map-tools/SKILL.md`, `.cursor/skills/sight-client-security/SKILL.md`. If KML is not sanitized, park — do not add DOMPurify in production. Never `innerHTML` of raw KML.

OnPush components: use `fixture.componentRef.setInput(...)` before `detectChanges` (entity-info lesson).
