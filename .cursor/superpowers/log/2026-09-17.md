# 2026-09-17

## Agent scaffolding (Sight + Superpowers)

- **Цель:** инициализация Superpowers/Cursor для репо без изменения `src/`.
- **Спека / план:** нет плана. Спеки as-is: `specs/2026-09-17-sight-product-design.md` и четыре дочерние.
- **Скилы:** каталог `.cursor/skills/sight-*` (создан в этом чате; SDD ещё не гоняли).
- **По существу:** `AGENTS.md`, `.cursorignore`, `.cursor/rules/`, `.cursor/skills/`, `.cursor/superpowers/` (specs, log, README). `.gitignore`: `.worktrees/`. Код приложения не менялся.
- **Standing decisions:** пути артефактов не в `docs/`; SDD-brief — 1–3 скила; standing decision и смена контракта продукта → владеющий файл/спека; журнал сессий → `log/YYYY-MM-DD.md`; `/save-chat-text` → `.chats/` (gitignore + cursorignore).
- **Статус:** done (обвязка). Следующее: коммит агентных файлов, новый чат, узкий прогон по одной дочерней спеке.

## As-is product code review

- **Цель:** read-only ревью текущего дерева `src/` против as-is спек и соглашений; код не менять.
- **Спека / план:** `specs/2026-09-17-sight-product-design.md` + viewer-crs, map-tools, ui-theme, github-pages.
- **Скилы:** `.cursor/skills/sight-change-control/SKILL.md`, `.cursor/skills/sight-client-security/SKILL.md`, `.cursor/skills/sight-angular-ui/SKILL.md` (плюс cesium/map-tools/geodesy/testing/a11y по подсистемам).
- **По существу:** отчёт в чате. Главное: зона СК-42 м ломается на `zone === ''`; KML/`billboard.image` и drag-drop CZML без проверки URL; нет `coord-sistems.lib.spec.ts`.
- **Standing decisions:** нет (ревью, не внедрение).
- **Статус:** done. Код не правился. `ng test` в этом прогоне не запускался.

## SK-42 m zone fix

- **Цель:** автозона СК-42 м при `zone === ''` и сохранение явной зоны; тесты на CoordSystems.
- **Спека / план:** viewer-crs as-is (`coord-sistems.lib.ts`). Плана нет.
- **Скилы:** `.cursor/skills/sight-geodesy/SKILL.md`, `.cursor/skills/sight-testing/SKILL.md`.
- **По существу:** `coord-sistems.lib.ts` (нормализация `''`, duck-type lon/lat); `coord-sistems.lib.spec.ts`. Ловушка зоны записана в `sight-geodesy`.
- **Standing decisions:** `''`/`undefined` = автозона СК-42 м; не `instanceof CartographicLike` для POJO — скил геодезии.
- **Статус:** done. Karma: 3/3 SUCCESS на этом spec. Полный `ng test` по-прежнему не собирается из-за старых ошибок в других spec.
